module.exports = function () {

    var lib = 'lib/';
    var src = 'src/';
    var build = 'dist/';

    var config = {
        build: build,
        lib: lib,
        src: src,
    }

    return config;
};
