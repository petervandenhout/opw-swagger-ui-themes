module.exports = function () {

    var lib = 'lib/';
    var demo = 'demo/';
    var src = 'src/';
    var build = 'dist/';

    var config = {
        build: build,
        demo: demo,
        lib: lib,
        src: src,
    }

    return config;
};
